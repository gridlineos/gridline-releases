# ada-copilot-releases
Public download artifacts for the ADA Copilot for Revit plugin. Plugin source + proxy live in the private revit-ada-copilot monorepo; this repo only holds the per-release DLL zips so the in-plugin updater can download them without auth.
